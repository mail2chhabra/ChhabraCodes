---
 - hosts: host1
   connection: local
   tasks:
   - name: add group "tomcat"
     group: name=tomcat

   - name: add user "tomcat"
     user: name=tomcat group=tomcat home=/usr/share/tomcat createhome=no
     become: True
     become_method: sudo

   - name: Copy Tomcat
     copy: src=/opt/software/apache-tomcat-8.5.38.tar.gz dest=/opt/apache-tomcat-8.5.38.tar.gz

   - name: Extract archive
     command: chdir=/usr/share /bin/tar xvf /opt/apache-tomcat-8.5.38.tar.gz -C /opt/ creates=/opt/apache-tomcat-8.5.38

   - name: Symlink install directory
     file: src=/opt/apache-tomcat-8.5.38 path=/usr/share/tomcat state=link

   - name: Change ownership of Tomcat installation
     file: path=/usr/share/tomcat/ owner=tomcat group=tomcat state=directory recurse=yes


